/**
 * New node file
 */

var express = require('express');
var router = express.Router();
/*
var Device = require('./Add/Device');
var Partition = require('./Add/Partition');

router.use('/Device', Device);
router.use('/Partition', Partition);
*/

module.exports = router;